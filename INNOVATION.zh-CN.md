**简体中文** · [English](INNOVATION.md)

# 创新说明

本文件回答一个问题:**F3-OpenScience 里,哪些是原创的,哪些是整合的?**

把这两者分清楚,比笼统宣称"创新"更有价值 —— 也更符合本项目自身的原则:
不把自己没做的事算作自己的。

---

## 一、整合的部分(致谢,非原创)

| 能力 | 来源 | 我们做了什么 |
|---|---|---|
| 广度:模型无关、多技能、科学数据库 | [OpenScience](https://github.com/synthetic-sciences/openscience)(Apache-2.0) | 复用其设计思路 |
| 可信交付:跨平台桌面、可审计工作区 | [Open Science Desktop](https://github.com/ai4s-research/open-science) | fork 壳层 |
| 双持久记忆、记成功也记失败 | EvoScientist(arXiv 2603.08127) | 复现设计(论文无开源实现) |
| 4 层引用核验 + 反捏造 | AutoResearchClaw(arXiv 2605.20025) | 复现机制(自研实现,绕开许可证) |
| 证据链全覆盖门控的思路 | ScientistOne(arXiv 2605.26340) | 借鉴思路,自研实现 |

**这些不是我们的发明。** 我们的工作是把它们拼成一个能跑通"敢署名"的整体。

---

## 二、原创的部分:可达性框架

真正属于本项目的贡献,集中在一个问题上:

> **当一个系统「校验产出 → 沉淀经验 → 约束下次生成」时,
> 校验器的能力边界会悄悄变成生成器的世界边界。**

这个自毁机制此前没有被明确指出,更没有被系统性地防住。以下五点是原创。

### 创新点 1 — 失败的语义分野

现有系统把校验失败当作单一信号。我们证明它必须分成两类,且**只有一类有资格约束生成**:

| 类别 | 含义 | 可否回写为生成约束 |
|---|---|---|
| `fabrication` | 权威登记处确证不存在 —— **世界不允许** | ✅ |
| `verification_gap` | 索引未覆盖 / 判据不适用 —— **只是我看不到** | ❌ |

判据落在**证据基质的权威性**上:arXiv ID 与 DOI 登记处是稠密且权威的空间,
格式合法却解析不到即为捏造;而 OpenAlex 这类索引只能确认、**无权证伪**。

落地为一行:

```sql
inject()  WHERE lesson_class = 'fabrication'   -- 校验缺口不得进入生成约束
```

**关键**:署名门槛未放松 —— 两类失败都阻断署名。改变的只是它是否塑造生成偏好。
校验缺口转为 `capability_backlog()`(能力建设需求),而非生成禁区。

### 创新点 2 — 裁判从书目学迁向计算与物理

如果校验只会查数据库,"可达"就永远等于"被收录过"。我们把判据往下推,
使多数判据**完全不依赖文献索引**:

| 判据 | 权威否定源 | 依赖索引 |
|---|---|---|
| arXiv / DOI 登记处 | 确证不存在 | 是 |
| OpenAlex 索引 | 只覆盖,无权否定 | 是 |
| **推导重算** | 推导式与声明值矛盾 | **否 · 计算** |
| **量纲 / 取值域** | 准确率 > 1、量纲不可加 | **否 · 物理** |
| **领域物理** | 原子不守恒、价键超上限 | **否 · 物理** |

`H2 + O2 -> H2O` 原子不守恒是纯算术;`C2H8` 不饱和度为 -1 意味着氢超过骨架承载上限。
**任何数据库都改变不了它们。** 这是"可达性回归物理"的实际落点。

同时它**扩大**了可达空间:数字不再要求字面出现在日志中,只要能被重算即可。
放宽的是证据形式,不是证明责任。

### 创新点 3 — 物理约束前置重塑提议空间

量纲检查放在推导发现的**求值之前**:

```python
ok, _ = dims.check_expression(expr, symbols)
if not ok: continue      # 物理上不可能的组合根本不进入候选
```

不是"先自由拟合、再事后过滤",而是用硬约束重塑搜索空间本身。

### 创新点 4 — 三曲线并读的诚实指标

只报"拦截率下降"无法区分两件事:系统学会了不再捏造,还是学会了绕开难题。
必须三条曲线同读:

```
拦截率↓ + 可达率持平/↑ + 探索率持平/↑  =  learning       真的学会了
拦截率↓ + 可达率↓                        =  narrowing      ⚠ 绕开难校验区域
拦截率↓ + 可达率持平 + 探索率↓            =  conservative   ⚠ 只敢走熟路
```

第三种最隐蔽:所有校验指标都好看,但系统已不再提出低先验假设,
退化成一台**安全的平庸机器**。只看前两条曲线永远发现不了。

配套的**可达性回归集**(`tests/golden/reachability_case.json`)专门收集
"世界允许但校验器容易误杀"的论断 —— 它的价值不是刷高分,而是暴露边界。
`false_rejection_rate` 上升的改动不予合并,即使它提高了拦截率。

### 创新点 5 — 新颖论断可凭计算证据署名

补上一个此前完全失明的区域:像"我们提出机制 X 导致 Y"这类**真正的科学主张**,
以前既不被拦截也不被背书,系统却在为整篇草稿署名。

新颖度决定**所需证据基质**,而非能否通过:

```
文献中有支撑    →  引用型或计算型证据均可
索引中未见支撑  →  必须由计算型证据背书(可复现包 / 推导 / 运行日志)
```

第二条不是放宽,而是换了一种**更硬**的基质。这使系统第一次能为
"文献里没有的东西"负责,而非退化成查重器。

配合**探索预算**(低先验假设强制配额),主动往模型不喜欢的区域走 ——
但探索**不降低证据标准**:低先验假设同样要过计算与物理的裁判。
鼓励探索而不设物理裁判,只会放大噪声。

---

## 三、一条贯穿全项目的方法论

开发过程中,同一个认识论错误在**四个不同层级**各出现一次:

| 层级 | 错误形态 |
|---|---|
| 基础设施 | 熔断器把 HTTP 404(登记处说"没有")与网络超时(连不上)混为一谈 |
| 引用校验 | 把"OpenAlex 查不到"当成"这篇论文不存在" |
| 量纲推断 | 文本同时含 accuracy 与 improvement 时武断择一,把合法的 12.4% 提升判成"概率超过 1" |
| 领域判据 | 自由基 `CH3` 的不饱和度是半整数 —— 它真实存在,不该判死 |

四次都是同一件事:**把自己的可观测边界,误当成世界的边界。**

因此整套代码贯彻一条纪律:

> **未知就是未知,不伪装成已知,也不伪装成不可能。**

语义有歧义 → 不下断言;判据不适用 → 承认判断不了;库没装 → 明写"能力缺失,非物理结论"。

我们认为这条方法论的适用范围**不限于科研 Agent** ——
任何带校验闭环的 AI 系统都会遇到同构的问题。

---

## 四、可复现的验证

以下均可在本仓库直接复跑(`make test`,14 个测试套件):

| 主张 | 验证方式 |
|---|---|
| 0 幻觉引用漏放 | `tests/test_coe.py` 金标集,打真实 arXiv / CrossRef / OpenAlex |
| 0 误拒率 | `tests/test_reachability.py`,含专设的误拒陷阱 |
| 飞轮只被捏造类约束 | `tests/test_flywheel.py` + `test_reachability.py` |
| 推导重算与矛盾检测 | `tests/test_derivation.py`(含 AST 白名单安全测试) |
| 量纲不可加 / 取值域 | `tests/test_dimensions.py` |
| 质量守恒 / 价键上限 | `tests/test_domains.py`(守恒判据零依赖) |
| 三曲线判定 | `tests/test_exploration.py` |

**请同时阅读 [STATUS.md](STATUS.zh-CN.md) —— 其中明确列出了尚未验证的部分。**
