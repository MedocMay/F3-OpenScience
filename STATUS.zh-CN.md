**简体中文** · [English](STATUS.md)

# 项目状态与验证边界

> **本项目在资源受限条件下开发,尚未经过全面测试。**
> 请在了解以下边界后再决定如何使用。

本文件的存在本身就是项目原则的一部分:
**未知就是未知,不伪装成已知。** 我们要求代码承认它判断不了的东西,
也就必须对项目自身适用同一标准。

---

## 一、成熟度定位

**这是一个可运行的研究原型(research prototype),不是生产就绪的产品。**

适合:
- 研究校验机制、可达性框架的技术思路
- 在受控环境中试用与二次开发
- 作为「校验 + 记忆」类系统的参考实现

不适合(在完成相应验证前):
- 直接用于产出会真正署名发表的论文
- 面向不可信用户的公开多租户服务
- 任何对正确性有强保证要求的关键场景

---

## 二、已实测的部分

以下均可在本仓库复跑 —— 跑 `make test`(14 个套件)并逐行看:某个套件若够不着服务,会打印「(0 ran · N SKIPPED, not run)」而不是干净的通过。

| 项 | 验证方式 | 边界 |
|---|---|---|
| 4 层引用核验 | 打真实 arXiv / CrossRef / DataCite / OpenAlex | 金标集规模小(个位数样本) |
| 推导重算 + AST 安全 | 单元测试,含注入攻击用例 | 算子空间仅 7 种 |
| 量纲 / 取值域 | 单元测试 | 仅覆盖 ML/CS 常见量,命名启发式 |
| 化学守恒 / 不饱和度 / 价键 | 单元测试 | 仅 C/H/N/O/卤素;未对真实化学工作流验证 |
| 飞轮分流与经验库治理 | 端到端测试 | 合成数据,非真实多用户 |
| 跨用户 global 聚合 | HTTP 端到端 | 单机模拟多用户 |
| 多进程 IPC(TS 大脑 + Python sidecar) | 实跑 | — |
| 沙箱隔离(环境擦除 / 资源限额 / 超时) | 实测拦截内存炸弹、死循环、密钥读取 | 仅 Linux 完整;见下 |
| 三种部署的服务层 | 纯净 venv 内实测 | Docker 编排层未跑,见下 |

---

## 三、**未验证**的部分(重要)

### 🟡 用真实 LLM 端到端跑通过一次 —— 2026-07-29

在此日期之前,上面这条写的是「从未」。障碍不是缺凭据:流水线根本没调过
`ModelRouter`,draft 是字符串拼出来的。接上线并对 DeepSeek 跑通后,
`draft_source: deepseek:deepseek-chat`,数字取自沙箱日志而非编造,引用经核验为真。

那次运行**没有**确立什么,以及它为什么是 🟡 而不是绿:

- **一次运行、一个供应商、一个方向。** 这里没有任何东西构成分布。
- 第一次运行**没有出现幻觉,是因为不可能出现** —— prompt 把检索到的论文交给了模型,
  它照着引。那证明的是检索增强有效,不是校验层有效。
- 拿掉检索这根拐杖后,模型的失效模式是**张冠李戴,不是捏造**:它引用
  arXiv:2305.18290 —— 真实且被引上万次的 DPO 论文 —— 去支撑一个关于数据稀缺领域
  少样本分类的主张,而那篇论文对此只字未提。权威登记处抓不住这个,因为登记处确认
  它存在。存在性与支撑性是两种不同的属性。
- 第 4 层确实拦下了它(`manual`,相关性 0.00),而对同一篇论文的准确引用则通过。
  那是 **n=1**,见下条。
- `_EXPERIMENT` 仍是固定 seed 的占位脚本。模型写的是一个合成结果。

### 🔴 第 4 层只有在配置了模型时才是硬门控

`get_scorer()` 在有可用供应商时返回 LLM 打分器,否则返回词重叠启发式。启发式
**只标注、放行**(`low_relevance: True`),只有 LLM 后端会阻断。因此在任何没有模型
凭据的部署里,一条张冠李戴的引用 —— 真实论文被用来支撑它没说过的事 —— 会畅通无阻
地抵达签名门。

徽章 `no fabricated citation passes the signing gate` 字面属实,因为张冠李戴不是
捏造。但它仍然会引出一个强于代码所能支撑的读法。配好供应商后设
`COE_RELEVANCE=llm`,可让第 4 层成为硬门控。

**已对真实模型实测,2026-07-29(DeepSeek,`experiments/relevance/run_batch.py`):**
九篇真实论文,每篇配一条取自其自身摘要的准确引用、一条取自另一篇真实论文摘要的
张冠李戴 —— 地面真值任何人打开两个链接即可复核,不依赖任何人的一面之词。

    张冠李戴被拦   9/9      漏放   0
    准确引用通过   9/9      误拒   0

此处两个方向都报,是因为一个调到能抓住每一条张冠李戴的打分器会开始拦准确引用,
而只引用其中一个数字,正是把门控往任一方向调废的标准做法。九仍然只是九。

**这组数字之所以存在,是因为先把判定变成了可复现的。** 本批量的第一次运行,第二行
报的是 8/9。那一例看似的误拒既不是坏用例,也不是真误判:代码库里任何地方都没有设过
`temperature`,于是相关性判定是**采样**出来的。对完全相同的输入打分八次,那条边界论断
返回 0.6、1.0、0.0、1.0、0.0、0.8、0.9、0.9 —— 两次越过 0.10 阈值。同样八次调用下,
判然分明的输入是稳定的(一条张冠李戴每次都打 0.0),所以信号是真的,方差集中在真正
模棱两可的论断上。相关性调用现已钉死 `temperature=0`,此后同一条论断八次全打 0.8。

**一个答案取决于抽签的签名门,不成其为门**,而本项目此前既没有对应的设置,也没有对应的
指标。零温度是托管 API 所能提供的最强请求,不是保证;残余方差记录在
`layers["llm_relevance"]` 里,而非被隐藏。**判定可复现性理应与误拒率、漏放率并列
作为一项被报告的属性,目前它不是。** 这是待办工作。

### 🟡 检索找到的是相邻工作,而流水线声称过头

`literature()` 原先用 `all:<整句>`,字段前缀只作用于第一个词,其余松散匹配。于是
「few-shot reinforcement learning for battery health」返回了一篇联邦学习做
**电子健康病历**的论文 —— 命中的是 health 这个词。三级查询阶梯(加引号的短语 →
中心语 → 原松散形式,并以 `_match` 记录命中级别)修掉了其中最糟的部分。

剩下的不是检索缺陷。`hypothesize()` 给每篇检索到的论文贴上 `prior work on {direction}`
—— 一个流水线并无依据的断言。一篇仅仅相邻的论文(方向是 few-shot RL 做电池健康,
而论文讲的是传感网里的电池退化管理)并不是该方向的前作,第 4 层这么判是对的。
模板路径没有「用某篇论文**支撑**某个具体主张」这个概念,而那正是引用的定义。
区分 `support` 与 `context` 两类论断是待办工作。

### 🔴 从未产出过一篇真实研究成果

MVP 的验收标准原本包含「3–5 个真实 ML 题目,产出可署名草稿」。
**这一步没有做。** 所有测试用的都是构造的论断与合成实验。

因此「拦截率 [1,0,0]」「误拒率 0」这些数字,反映的是**在设计的测试集上**的表现,
不能外推为真实科研场景下的效果。

### 🟡 桌面应用能构建,但从未运行过

*2026-07-25 更正。本节此前写「从未编译过」—— CI 构建成功后该表述已失效。*

- **构建是成功的。** v0.2.0 的 release 工作流在 ubuntu-22.04(`.AppImage` / `.deb`)、
  macos-latest(`.dmg`)、windows-latest(`.msi` / `.exe`)上都产出了安装包。
- **运行从未被验证。** 没有人装过其中任何一个、打开过那个窗口。
  编译成功只说明 Rust 与前端构建是通的,**不说明** UI 能用、
  sidecar 在干净机器上能正常起来、启动页到主界面的切换是否正常。
- **Intel macOS 没有预编译安装包。** 该 CI runner 排队可能长达数小时、会拖住整个发布,
  因此不构建。Intel 用户需本地自行构建。
- macOS 的 `.dmg` **未签名、未公证** —— 普通 Mac 上会被 Gatekeeper 拦住。
  Windows 安装包同样未签名,会触发 SmartScreen 警告。
- 壳↔orchestrator 的 IPC 协议是用 Node 模拟壳验证的,不是用真实 GUI。

### 🟡 Docker / 编排层从未运行

- 开发环境无 Docker。`compose.local.yml` / `compose.cloud.yml` / `compose.prod.yml`
  只做了 YAML 结构与依赖一致性校验,**从未 `docker compose up` 过**。
- Postgres 适配写成了同接口可切换,但**只在 SQLite 后端实测**,
  Postgres 路径未运行(无 psycopg 环境)。
- Redis 会话外置同理:代码就绪,只测过内存回退分支。
- Caddy 自动 TLS 未验证。

### 🟡 跨平台仅 Linux 实测

- **Windows / macOS 的安装与启动脚本只做了静态校验**(语法、占位符替换、结构),
  从未在真实 Windows / macOS 上运行。
- 沙箱在 Windows 上**没有内存与进程数限额**(POSIX `setrlimit` 特性),
  降级为环境擦除 + 目录隔离 + 超时终止。面向不可信用户的服务请部署 Linux。
- 网络隔离(`unshare -n`)仅 Linux 可用。

### 🟡 规模与性能完全未测

- 未做任何压力测试、并发测试、长时间运行测试。
- 经验库随规模增长的行为(索引、衰减、查询性能)未验证。
- global 服务在真实多租户并发下的表现未知。
- 未做内存泄漏与资源占用分析。

### 🟡 安全未经独立审计

- 沙箱、AST 白名单求值、BYOK 密管、多租户隔离**均为自测**,
  **未经第三方安全审计或渗透测试**。
- 推导式求值接受模型生成的表达式,是明确的攻击面。
  我们做了白名单防护并测试了常见绕过,但**不能保证没有未知绕过**。
- 生产部署前请自行审计,并参照 [SECURITY.md](SECURITY.zh-CN.md) 的部署清单。

### 🟡 领域判据覆盖极窄

- 化学:仅原子守恒、不饱和度(限 C/H/N/O/卤素)、RDKit 价键。
  **没有**热力学可行性、动力学、立体化学、反应条件等。
- 生物、材料等领域**完全没有实现**,仅预留了接口。

---

## 四、已知的方法论局限

即使在已实现的范围内,以下是设计上的**固有边界**,不是待修的 bug:

1. **CoE 不判断因果是否成立。** 它只校验「证据基质是否成立」。
   一个实验是否**充分证成**某个机制命题,由人在署名前 GATE 复核。

2. **量纲推断依赖命名启发式。** 它不是物理定律。
   我们让它在歧义时返回 `unknown` 而非武断判定,但仍可能漏判。

3. **自动推导发现是保守的。** 只搜 7 个有意义的算子,精度不足即放弃 ——
   会漏掉合法推导,但避免把噪声拟合成"证据"。合法推导可由作者显式声明。

4. **索引覆盖不足与真正新颖在证据上无法区分。** 因此系统只报
   `unestablished_in_index`,绝不断言 `novel`。

---

## 五、我们希望社区帮忙验证什么

按价值排序:

1. **用真实 LLM 跑通端到端**,并报告校验层的真实误拒 / 漏放率。
   核心论断的 A/B 实验装置已备好:[`experiments/narrowing/`](experiments/narrowing/) ——
   目前用替身生成器,**换成真实模型就是把论点变成发现的那个实验**。
2. **在真实研究题目上试用**,尤其欢迎「校验器误杀了真东西」的案例
   → 请提 [误拒报告](.github/ISSUE_TEMPLATE/false-rejection.yml)
3. **构建并试用桌面应用**(需 Rust 工具链)
4. **在真实 Docker / Postgres / 多租户环境部署**并报告问题
5. **安全审计**,尤其是沙箱与推导式求值
6. **扩展领域判据**(生物、材料),或指出化学判据的错误

---

## 六、版本承诺

- 0.x 阶段**不保证 API 稳定**,契约可能有破坏性变更(见 [CHANGELOG.md](CHANGELOG.md))
- 我们不会为了让指标好看而放宽断言
- 发现的缺陷会如实记入 CHANGELOG,包括我们自己犯的错

---

*最后更新:v0.2.0 · 如果你发现本文件与实际情况不符,那本身就是一个值得提的 Issue。*
